import { Component } from '@angular/core';


@Component({
  moduleId: module.id,
  selector: 'my-app',
  template: `
            
            `
})
export class AppComponent {
  name: string = "World";
}
